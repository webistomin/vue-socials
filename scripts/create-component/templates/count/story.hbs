import { Story } from '@storybook/vue';
import { action } from '@storybook/addon-actions';
import { TBaseCountPropsOptions } from '@/mixins/BaseCount/BaseCount';
import {{pascalCase name}}, { I{{pascalCase name}}ShareOptions } from '../{{pascalCase name}}';

export default {
  title: '{{pascalCase type}}/{{pascalCase name}}',
  component: {{pascalCase name}},
};

const Template: Story<TBaseCountPropsOptions<I{{pascalCase name}}ShareOptions>> = (_args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { {{pascalCase name}} },
  methods: {
    onLoad: action('emit load'),
    onLoading: action('emit loading'),
    onError: action('emit error'),
  },
  template: `
    <{{kebabCase name}}
      class="base-social"
      v-bind="$props"
      @load="onLoad"
      @error="onError"
      @loading="onLoading"
    >
    </{{kebabCase name}}>
  `,
});

export const Default = Template.bind({});
Default.args = {
  tag: 'span',
  shareOptions: {
    url: 'https://github.com/',
  },
};
