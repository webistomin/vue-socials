import { Story } from '@storybook/vue';
import { action } from '@storybook/addon-actions';
import { DEFAULT_WINDOW_FEATURES, TBaseSocialPropsOptions } from '@/mixins/BaseSocial/BaseSocial';
import {{pascalCase name}}, { I{{pascalCase name}}ShareOptions } from '../{{pascalCase name}}';

export default {
  title: '{{pascalCase type}}/{{pascalCase name}}',
  component: {{pascalCase name}},
};

const Template: Story<TBaseSocialPropsOptions<I{{pascalCase name}}ShareOptions>> = (_args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { {{pascalCase name}} },
  methods: {
    onClose: action('emit close'),
    onOpen: action('emit open'),
    onBlock: action('emit block'),
    onFocus: action('emit focus'),
  },
  template: `
    <{{kebabCase name}}
      class="base-social"
      v-bind="$props"
      @popup-close="onClose"
      @popup-open="onOpen"
      @popup-block="onBlock"
      @popup-focus="onFocus"
    >
      Share
    </{{kebabCase name}}>
`,
});

export const Default = Template.bind({});
Default.args = {
  windowFeatures: DEFAULT_WINDOW_FEATURES,
  shareOptions: {
    url: 'https://github.com/',
  },
  useNativeBehavior: false,
};
