/**
* Hey!
*
* {{pascalCase name}} component used for ... social network
* @link ...
*/

import Vue, { VueConstructor } from 'vue';
import JSONP from '@/utils/jsonp';
import getSerialisedParams from '@/utils/getSerialisedParams';
import BaseCount, { TBaseCountMixin } from '@/mixins/BaseCount/BaseCount';

/**
* Share parameters for link
* @link ...
*/
export interface I{{pascalCase name}}ShareOptions {

}

export default /* #__PURE__ */ (Vue as VueConstructor<Vue & InstanceType<TBaseCountMixin<I{{pascalCase name}}ShareOptions>>>).extend({
  name: '{{pascalCase name}}',

  mounted() {},
});
