/**
 * Hey!
 *
 * {{pascalCase name}} component used for ... social network
 * @link ...
 */

import Vue, {
  CreateElement, VNode, VueConstructor,
} from 'vue';
import BaseSocial, { TBaseSocialMixin } from '@/mixins/BaseSocial/BaseSocial';
import getSerialisedParams from '@/utils/getSerialisedParams';

/**
 * Share parameters for link
 * @link ...
 */
export interface I{{pascalCase name}}ShareOptions {

}

export default /* #__PURE__ */ (Vue as VueConstructor<Vue & InstanceType<TBaseSocialMixin<I{{pascalCase name}}ShareOptions>>>).extend({
  name: '{{pascalCase name}}',

  mixins: [BaseSocial<I{{pascalCase name}}ShareOptions>(
    '',
  )],

  computed: {
    networkURL(): string {
      const BASE_URL = '';
      const { shareOptions } = this;
      const { } = shareOptions;
      const serialisedParams = getSerialisedParams({});

      return `${BASE_URL}${serialisedParams}`;
    },
  },

  render(h: CreateElement): VNode {
    return this.generateComponent(h, this.networkURL);
  },
});
