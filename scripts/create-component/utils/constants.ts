import path from 'path';

export const WORKING_PATH = process.cwd();

export const SRC_FOLDER = path.resolve(WORKING_PATH, './src');

export const COMPONENTS_FOLDER = path.resolve(SRC_FOLDER, './components');
